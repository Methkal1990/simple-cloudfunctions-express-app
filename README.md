# simple-cloudfunctions-express-app
an express app to be deployed to firebase functions

# How to use:

1- change the FIREBASE_PROJID in .firebaserc to your project id
2- deploy with `firebase deploy --only functions`
